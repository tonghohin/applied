{
  "name": "@repo/ai",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.8.3",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^3.0.119",
    "@ai-sdk/mcp": "^1.0.43",
    "@playwright/mcp": "^0.0.75",
    "@repo/db": "workspace:*",
    "ai": "^6.0.190",
    "playwright": "^1.60.0",
    "zod": "^4.4.3"
  }
}
